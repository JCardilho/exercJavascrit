// Seleciona o primeiro elemento com a classe "destaque"
var elementoDestaque = document.querySelector('.destaque');

// Verifica se o elemento foi encontrado
if (elementoDestaque) {
    // Aplica o estilo de fundo amarelo
    elementoDestaque.style.backgroundColor = 'yellow';
}